# Bijdragen

1. Bewerk alleen `content-source/`, `layouts/`, `static/` (niet `static/vsa/`), `scripts/`.
2. Lokaal: `scripts\bootstrap.cmd` daarna `scripts\serve-hugo.cmd`.
3. Conventional Commits; push naar `main` triggert Pages-deploy.
